---
layout: base.njk
title: Christian Apologetics Talks
description: Equipping Believers with Reasoned Faith — One Talk at a Time.
---

<!-- Hero Section -->
<section class="relative bg-gradient-to-br from-logo-navy via-logo-steel to-logo-navy py-20">
    <div class="max-w-7xl mx-auto text-center">
        <div class="mb-8">
            <h1 class="text-4xl md:text-6xl font-bold text-vs-fg mb-6">
                The Developing Apologist
                <span class="text-logo-blue">Talks</span>
            </h1>
            <p class="text-xl md:text-2xl text-gray-300 max-w-3xl mx-auto leading-relaxed">
                Equipping Believers with Reasoned Faith — One Talk at a Time.
            </p>
        </div>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <div class="flex items-center space-x-2 text-logo-glow">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span class="text-sm font-medium">{{ talks.length }} Presentations Available</span>
            </div>
            <div class="flex items-center space-x-2 text-logo-orange">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
                </svg>
                <span class="text-sm font-medium">Free Resources</span>
            </div>
        </div>
    </div>
</section>

<!-- Filter Section -->
<section class="bg-logo-steel border-b border-logo-circuit py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
            <div>
                <h2 class="text-2xl font-bold text-vs-fg mb-2">All Presentations</h2>
                <p class="text-gray-400">Browse our collection of Christian apologetics talks</p>
            </div>
            
            <!-- Filter by difficulty -->
            <div class="flex flex-wrap gap-2">
                <button class="filter-btn btn-secondary active:bg-logo-blue active:text-white" data-filter="all">All Levels</button>
                <button class="filter-btn btn-secondary active:bg-logo-blue active:text-white" data-filter="Beginner">Beginner</button>
                <button class="filter-btn btn-secondary active:bg-logo-blue active:text-white" data-filter="Intermediate">Intermediate</button>
                <button class="filter-btn btn-secondary active:bg-logo-blue active:text-white" data-filter="Advanced">Advanced</button>
            </div>
        </div>
    </div>
</section>

<!-- Talks Grid -->
<section class="py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-7xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8" id="talks-grid">
            {% for talk in talks %}
            <article class="talk-card" data-difficulty="{{ talk.tags | first }}">
                <!-- Talk Header -->
                <div class="mb-4">
                    <h3 class="text-xl font-bold text-vs-fg mb-2">{{ talk.title }}</h3>
                    <p class="text-gray-400 text-sm mb-3">{{ talk.description }}</p>
                </div>

                <!-- Tags -->
                <div class="mb-4">
                    {% for tag in talk.tags %}
                    <span class="tag">{{ tag }}</span>
                    {% endfor %}
                </div>

                <!-- YouTube Embed (if available) -->
                {% if talk.youtube %}
                <div class="mb-4">
                    <iframe 
                        class="youtube-embed" 
                        data-src="{{ talk.youtube }}"
                        title="{{ talk.title }}"
                        frameborder="0" 
                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                        allowfullscreen>
                    </iframe>
                </div>
                {% else %}
                <div class="mb-4">
                    <div class="youtube-embed bg-logo-navy flex items-center justify-center">
                        <div class="text-center text-gray-400">
                            <svg class="w-12 h-12 mx-auto mb-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z" clip-rule="evenodd"></path>
                            </svg>
                            <p class="text-sm">Video Coming Soon</p>
                        </div>
                    </div>
                </div>
                {% endif %}

                <!-- Talk Meta -->
                <div class="talk-meta">
                    <div class="flex items-center justify-between text-xs">
                        <span>{{ talk.date | dateFilter }}</span>
                        <span>{{ talk.duration }}</span>
                    </div>
                </div>

                <!-- Action Links -->
                <div class="talk-links">
                    {% if talk.slides %}
                    <a href="{{ talk.slides }}" class="talk-link slides">
                        <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M3 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clip-rule="evenodd"></path>
                        </svg>
                        View Slides
                    </a>
                    {% endif %}
                    
                    {% if talk.blog %}
                    <a href="{{ talk.blog }}" class="talk-link blog">
                        <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M4 4a2 2 0 012-2h4.586A2 2 0 0112 2.586L15.414 6A2 2 0 0116 7.414V16a2 2 0 01-2 2H6a2 2 0 01-2-2V4zm2 6a1 1 0 011-1h6a1 1 0 110 2H7a1 1 0 01-1-1zm1 3a1 1 0 100 2h6a1 1 0 100-2H7z" clip-rule="evenodd"></path>
                        </svg>
                        Read Blog
                    </a>
                    {% endif %}
                </div>
            </article>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Call to Action -->
<section class="bg-logo-circuit py-16 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl font-bold text-vs-fg mb-4">Ready to Deepen Your Faith?</h2>
        <p class="text-xl text-gray-300 mb-8">
            Explore more resources and join the conversation on our main site.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="https://developingapologist.com" class="btn-primary">
                Visit Main Site
            </a>
            <a href="https://blog.developingapologist.com" class="btn-secondary">
                Read Our Blog
            </a>
        </div>
    </div>
</section>

<script>
// Filter functionality
document.addEventListener('DOMContentLoaded', function() {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const talkCards = document.querySelectorAll('.talk-card');
    
    filterBtns.forEach(btn => {
        btn.addEventListener('click', function() {
            const filter = this.dataset.filter;
            
            // Update active button
            filterBtns.forEach(b => {
                b.classList.remove('bg-logo-blue', 'text-white');
            });
            this.classList.add('bg-logo-blue', 'text-white');
            
            // Filter cards
            talkCards.forEach(card => {
                if (filter === 'all' || card.dataset.difficulty === filter) {
                    card.style.display = 'block';
                } else {
                    card.style.display = 'none';
                }
            });
        });
    });
    // Set initial active state
    const initialActive = document.querySelector('.filter-btn[data-filter="all"]');
    if (initialActive) {
        initialActive.classList.add('bg-logo-blue', 'text-white');
    }
});
</script> 